import { Player } from "../Objects/player";

export const ActionName: string = 'Action';

export default class Action{
    public name: string = ActionName;

    public doAction(ent: Player, opt: any){

    }
}